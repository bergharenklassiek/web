import { StoryBlokImagePipe } from './story-blok-image.pipe';

describe('StoryBlokImagePipe', () => {
  it('create an instance', () => {
    const pipe = new StoryBlokImagePipe();
    expect(pipe).toBeTruthy();
  });
});
